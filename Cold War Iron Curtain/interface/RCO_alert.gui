guiTypes = {
	# Same size/look as global_alerticon_window (interface/alerts.gui).
	# Sits one slot LEFT of alerticon_startposition (842,40), offset 48.
	# ponytail: fixed slot; if it overlaps something, change x here.
	containerWindowType = {
		name = "RCO_alert_window"
		position = { x = 855 y = 85 }
		size = { width = 47 height = 42 }
		moveable = no
		Orientation = UPPER_LEFT

		buttonType = {
			name = "RCO_alert_button"
			spriteType = "GFX_RCO_alert_icon"
			position = { x = 0 y = 0 }
			oversound = ui_menu_over
			clicksound = click_default
			pdx_tooltip = "RCO_alert_tt"
		}
	}
}
