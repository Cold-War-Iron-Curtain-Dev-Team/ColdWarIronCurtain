on_actions = {
	# ROOT = the country ticking -> the sender when RCO_sync runs.
	# original_tag so civil-war / dynamic SOV and USA are included.
	on_daily = {
		effect = {
			if = {
				limit = {
					OR = {
						original_tag = SOV
						original_tag = USA
					}
				}
				RCO_sync = yes
			}
		}
	}
}
