# Modifier definitions added to clear "Unknown modifier: <x>" load errors.
# These are script-only modifiers (modifier@<token>) -- defining them here gives the
# token a tooltip + category; it does NOT make the engine apply any built-in effect.
# color_type / category are best-guess from the calling context; adjust as needed.

air_defence = {
	color_type = good
	value_type = percentage
	precision = 2
	category = air
}

air_intel_to_others = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = intelligence_agency
}

arms_factory_throughput = {
	color_type = good
	value_type = percentage
	precision = 2
	category = war_production
}

army_experience_gain_factor = {
	color_type = good
	value_type = percentage
	precision = 2
	category = army
}

army_experience_gain_mult = {
	color_type = good
	value_type = percentage
	precision = 2
	category = army
}

assassination_success_rate = {
	color_type = good
	value_type = percentage
	precision = 2
	category = intelligence_agency
}

border_security_strength = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

city_infiltration_speed = {
	color_type = good
	value_type = percentage
	precision = 2
	category = intelligence_agency
}

civilian_fear_modifier = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = country
}

division_training_time = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = army
}

industry_research_speed = {
	color_type = good
	value_type = percentage
	precision = 2
	category = war_production
}

intel_gathering_speed = {
	color_type = good
	value_type = percentage
	precision = 2
	category = intelligence_agency
}

lend_lease_cost = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = country
}

local_production_factory_max_efficiency_factor = {
	color_type = good
	value_type = percentage
	precision = 2
	category = state
}

monthly_building_loss = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = state
}

occupation_cost = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = country
}

office_park_income_adjustment = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

political_advisor_cost_factor = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = politics
}

production_efficiency_gain_factor = {
	color_type = good
	value_type = percentage
	precision = 2
	category = war_production
}

radicalization_speed = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = politics
}

suppression_factor = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

trade_goods_cost = {
	color_type = bad
	value_type = percentage
	precision = 2
	category = country
}
