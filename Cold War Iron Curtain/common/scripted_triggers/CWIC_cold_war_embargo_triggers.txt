# ============================================================
# THE TWO SIDES OF THE 1949 TRADE WAR
#
# Used only by cwic_cold_war_embargo_startup, at game start. Edit the lists to
# change who begins the game cut off from whom; after that it is all content.
#
# West: the countries that actually operated the export controls - the US and
# the Western European states that formed COCOM in 1949, plus occupied Japan,
# which ran the embargo under SCAP.
#
# East: the Comecon founders of January 1949, plus the DDR and Mongolia.
# Yugoslavia is NOT here: it is already embargoed by the East in on_startup,
# Tito having been expelled in 1948, and putting it in either list would
# overwrite that.
# ============================================================

cwic_is_cold_war_west = {
	OR = {
		tag = USA  tag = ENG  tag = FRA  tag = CAN  tag = ITA
		tag = HOL  tag = BEL  tag = LUX  tag = POR  tag = NOR
		tag = DEN  tag = ICE  tag = TUR  tag = GRE  tag = JAP
	}
}

cwic_is_cold_war_east = {
	OR = {
		tag = SOV  tag = POL  tag = CZE  tag = HUN  tag = ROM
		tag = BUL  tag = ALB  tag = DDR  tag = MON
	}
}
